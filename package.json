{
  "name": "the-night-out",
  "private": true,
  "version": "0.5.0",
  "description": "An AngualarJS Web application to help users determine what they are going to on a free day.",
  "repository": "https://github.com/mklic17/night-out",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.9",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "start": "http-server app -a localhost -p 8000 -c-1",
    "prestart": "npm install",
    "postinstall": "bower install",
    "preupdate-webdriver": "npm install"
  }
}
